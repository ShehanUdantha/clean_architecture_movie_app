enum BlocStates {
  initial,
  loading,
  success,
  error,
}

enum CardTypes {
  vertical,
  horizontal,
}

enum MoviesList {
  upcoming,
  trending,
  popular,
}

enum FavoriteStatus {
  added,
  removed,
}
